# Large binaries — fetch from Drive or generate with
# scripts/colab_fetch_or_build.jl / scripts/build_train_pairs.jl
data/synthetic/*.h5
models/*.jld2

# Regenerated by `Pkg.instantiate()`. Do not commit: the previous Manifest
# pinned MTGeophysics to a machine-local path that breaks Colab/CI clones.
Manifest.toml

# Run / plot artifacts
results/*
!results/KNOWN_LIMITATIONS.md
figures/
run_VFSA2DMT_*/
scripts/run_VFSA2DMT_*/

.venv/
.mplconfig/

.DS_Store
*.jl.cov
*.jl.*.cov
*.jl.mem
